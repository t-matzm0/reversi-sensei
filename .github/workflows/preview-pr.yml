name: Deploy PR Preview

on:
  pull_request:
    branches: [develop]

env:
  NODE_VERSION: '20'

jobs:
  preview:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run linting
        run: npm run lint

      - name: Run type checking
        run: npm run type-check

      - name: Build for Firebase
        run: npm run build:firebase:staging
        env:
          BUILD_TARGET: firebase
          NEXT_PUBLIC_ENV: staging
          NEXT_PUBLIC_STAGING_PASSWORD: ${{ secrets.STAGING_PASSWORD }}

      - name: Install Firebase CLI
        run: npm install -g firebase-tools

      - name: Deploy to Firebase Preview Channel
        id: deploy
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_PRODUCTION }}
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
        run: |
          set +e  # Don't fail on individual command errors
          echo "$FIREBASE_SERVICE_ACCOUNT" > $HOME/gcloud-service-key.json
          export GOOGLE_APPLICATION_CREDENTIALS=$HOME/gcloud-service-key.json
          firebase hosting:channel:deploy pr-${{ github.event.pull_request.number }} --expires 7d --only reversi-sensei-dev --token "$FIREBASE_TOKEN" --json 2>&1 | tee deploy-output.json
          DEPLOY_EXIT_CODE=$?
          echo "=== Deploy Exit Code: $DEPLOY_EXIT_CODE ==="
          echo "=== Raw Deploy Output ==="
          cat deploy-output.json || echo "(empty file)"
          echo ""
          echo "=== Attempting URL extraction ==="
          # Try multiple extraction methods
          PREVIEW_URL=$(cat deploy-output.json | jq -r '.result["reversi-sensei-dev"].url // empty' 2>/dev/null || true)
          if [ -z "$PREVIEW_URL" ]; then
            PREVIEW_URL=$(cat deploy-output.json | jq -r '.result | to_entries[0].value.url // empty' 2>/dev/null || true)
          fi
          if [ -z "$PREVIEW_URL" ]; then
            # Try to extract URL with grep as fallback (use -E instead of -P for portability)
            PREVIEW_URL=$(grep -oE 'https://[^"]+\.web\.app' deploy-output.json 2>/dev/null | head -1 || true)
          fi
          echo "PREVIEW_URL=$PREVIEW_URL"
          echo "PREVIEW_URL=$PREVIEW_URL" >> $GITHUB_ENV
          # Save raw output for debugging (base64 encode to handle special chars)
          DEPLOY_OUTPUT_B64=$(head -c 1000 deploy-output.json 2>/dev/null | base64 -w0 || echo "")
          echo "DEPLOY_OUTPUT_B64=$DEPLOY_OUTPUT_B64" >> $GITHUB_ENV
          # Exit with deploy exit code to signal if deploy failed
          exit $DEPLOY_EXIT_CODE

      - name: Comment on PR
        if: always()
        uses: actions/github-script@v7
        with:
          script: |
            const url = process.env.PREVIEW_URL;
            const rawOutputB64 = process.env.DEPLOY_OUTPUT_B64 || '';
            let rawOutput = 'No output captured';
            if (rawOutputB64) {
              try {
                rawOutput = Buffer.from(rawOutputB64, 'base64').toString('utf8');
              } catch (e) {
                rawOutput = 'Failed to decode output';
              }
            }
            let body;
            if (url) {
              body = `🚀 Preview deployment ready!\n\n**Preview URL:** ${url}\n\nThis preview will expire in 7 days.`;
            } else {
              body = `⚠️ Preview deployment completed but URL extraction failed.\n\n<details>\n<summary>Debug: Raw deploy output</summary>\n\n\`\`\`\n${rawOutput}\n\`\`\`\n</details>`;
            }
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: body
            })
